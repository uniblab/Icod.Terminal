# A189 — 1.8.0 Package, Documentation, Compatibility, and Release Closure

**Release:** `Icod.Terminal 1.8.0`  
**Tranche:** A189  
**Status:** complete — accepted on exact qualification head `0de1a1c6b95b2b36407a94935b3126c8bfd6ca5a`, Staging workflow #1355 / `34476585310`

## Purpose

A189 is the final pre-merge qualification tranche for `Icod.Terminal 1.8.0`. It does not add another graphics feature. It synchronizes the package and permanent documentation with the already-qualified A180–A188 implementation, proves that the packed public contract remains the 1.7 raster surface, and requires the status-only final PR head to preserve that qualification before PR #46 leaves draft status.

## Accepted implementation checkpoints

| Tranche | Exact head | Staging workflow |
| --- | --- | --- |
| A180 | `88ac1db0a2904393622082423b8773bd8b19f121` | `34417006958` |
| A181 | `6b5abbdda8ac3ef57bf4c99054a65946e0f13b3a` | `34417741619` |
| A182 | `d9a02336aa2fd61b294064f526ade6021d9d35d2` | `34418298756` |
| A183 | `6f160610bf2df3c666e7d3350e7051475aceca36` | `34418863405` |
| A184 | `d3a7a9262263f28f7ae2d4511520f1b556e5539e` | `34420648720` |
| A185 | `c6a79421b8e8eb3c0c333493f5925f44a2931152` | `34422948067` |
| A186 | `21fb617965f41270e9f3cd43f405fa6ec6e87b2e` | `34426364013` |
| A187 | `80d78b74fe780421bf2e667ef434d38775c0dd48` | `34427249773` |
| A188 | `997feb9628d34389199ca3fffdf90e829f33819a` | `34469956370` |
| A189 qualification | `0de1a1c6b95b2b36407a94935b3126c8bfd6ca5a` | `34476585310` |

Every accepted checkpoint passed Runtime Windows, Runtime Linux, Runtime macOS, package candidate/public-API validation, Package Foundation, Package Presentation, Package Semantic and hardening, Package Stable 1.x release line, and the validated package artifact. A189 qualification additionally proves the final curated 1.8 release notes and synchronized overall development roadmap satisfy the release-line gates.

## Public API conclusion

Version 1.8 intentionally introduces **no public API additions**.

The authoritative current public API remains the 1.7 baseline:

```text
TerminalRasterPixelFormat
TerminalRasterColor
TerminalRasterImage
TerminalSession.DisplayRasterAsync(...)
```

Fingerprint:

```text
847441fb4a8cdc89979aca9e96178f939895b93ec19a973232210af09716f700
```

The authoritative files remain:

```text
docs/Public-API-Baseline-1.7.md
docs/Public-API-Baseline-1.7.sha256
```

A189 does not create a cosmetic `Public-API-Baseline-1.8` duplicate. The 1.8 compatibility result is precisely that the public surface did not change.

## Release behavior qualified

The single public raster operation now routes through two internal protocol backends:

```text
RasterGraphics
    -> verified ApcKittyGraphics
    -> verified DcsSixel
```

The qualified release behavior is:

- Kitty Graphics is an APC dialect beneath the existing semantic raster operation;
- direct raw transfer (`t=d`) is used;
- RGB24 and RGBA32 are transmitted directly;
- Indexed8 expands deterministically to RGB24 or RGBA32 as required by referenced palette alpha;
- Base64 payload chunks are bounded to 4096 bytes;
- the complete logical Kitty transfer is serialized as one committed session transaction even though it contains multiple APC frames;
- caller cancellation is honored before commitment and cannot intentionally truncate a committed transfer;
- partial transport failure is surfaced without replay or backend switching;
- Kitty support is established by a correlated protocol query plus Primary DA barrier, not terminal identity;
- verified Kitty is preferred and verified Sixel remains fallback;
- fractional alpha is preserved through Kitty and remains controlled unsupported through Sixel;
- source raster dimensions remain intrinsic; 1.8 adds no placement/scaling/cursor-normalization API;
- correlated malformed or oversized Kitty responses remain bounded transaction-owned input rather than leaking into application input.

## Package closure

The package generated from the A189 qualification head passed the complete package contract on all three target frameworks:

```text
net8.0
net9.0
net10.0
```

The dependency floor remains:

```text
Icod.TermInfo 1.10.0
Icod.Timing   1.0.0
```

The fresh NuGet-only raster consumer remains intentionally protocol-neutral. It proves that a consumer can compile and run against the same public `TerminalRasterImage` / `DisplayRasterAsync(...)` surface used by both 1.8 backends, that raster storage remains snapshot-owned, and that protocol-specific escape hatches are absent from the package.

The package smoke exclusion set covers both sides of the implementation:

- no raw/public DCS or Sixel writer;
- no raw/public APC or Kitty Graphics writer;
- no public protocol-specific raster display selector;
- no public mutable pixel/palette backing storage.

Normal runtime tests provide the internal multi-backend routing, probe, transaction, and hardening witnesses that a NuGet-only public-surface smoke project cannot access.

## Documentation closure

A189 synchronizes the primary release authorities:

- root `README.md`;
- `CHANGELOG.md`;
- `docs/releases/1.8.0.md`;
- `docs/Architecture.md`;
- `docs/Security-and-Privacy.md`;
- `docs/Compatibility-and-Versioning.md`;
- `docs/Semantic-Output-Protocols.md`;
- `docs/Control-Language-Normalization-and-Graphics-Roadmap.md`;
- `Icod.Terminal-1.8.0-Development-Roadmap.md`;
- `Icod.Terminal-Development-Roadmap.md`;
- NuGet package release notes;
- PR #46 tranche/acceptance ledger.

Historical A180–A188 contracts remain detailed evidence. Release-facing documents describe the current architecture directly so ordinary consumers do not need to reconstruct it from tranche history.

The curated `docs/releases/1.8.0.md` contains the release version, all three TFMs, and the required `Compatibility-and-Versioning.md` authority reference consumed by the Stable 1.x release-line verifier.

## Security closure

The permanent security authority now includes the 1.8-specific conclusions:

- terminal APC/Kitty replies are untrusted input;
- correlation establishes bounded ownership, not trust;
- matching response identifiers do not bypass grammar or size validation;
- malformed/aborted correlated responses fail the probe;
- oversized correlated responses are drained through bounded string resynchronization;
- an identified but unterminated response is malformed rather than silent uncertainty;
- unrelated control traffic does not satisfy the active probe;
- direct transport avoids hidden filesystem/temp-file/shared-memory side effects;
- graphics output is validated before commitment where possible;
- no automatic retry occurs after partial committed output;
- image-file decoding/transcoding remains out of scope.

## Compatibility closure

The permanent compatibility authority records that 1.8 is a behavioral/backend expansion beneath the unchanged 1.7 public raster API.

The release preserves:

- stable `1.0.0` compatibility floor;
- all public 1.0–1.7 members and enum numeric values;
- one-reader query ownership;
- bounded parser/query/resource behavior;
- truthful capability uncertainty;
- existing Sixel wire behavior and fallback role;
- session output ordering and committed-output integrity;
- lifecycle and restoration semantics;
- current downstream `Icod.DCurses` package-boundary compatibility witnesses.

## A189 qualification evidence

Exact qualification head:

```text
0de1a1c6b95b2b36407a94935b3126c8bfd6ca5a
```

Staging workflow:

```text
#1355 / 34476585310
```

The qualification passed:

- Runtime Windows;
- Runtime Linux;
- Runtime macOS;
- Package candidate/public-API freeze;
- Package Foundation;
- Package Presentation;
- Package Semantic and hardening;
- Package Stable 1.x release line;
- Validated package artifact.

Only status/evidence ledger edits follow this qualification. Those edits must themselves pass the same complete Staging matrix on the actual PR head before PR #46 is marked ready for review.

## Merge and publication rule

A green final Staging head is necessary for release readiness but does not merge or publish the release by itself.

The sequence remains:

```text
A189 qualification green under Staging
    -> status-only closure head green under Staging
    -> release program complete / PR ready for review
    -> merge only as explicitly directed
    -> merged main head must pass Release validation
    -> v1.8.0 tag/publication only after explicit authorization
```

No A189 documentation or package change is authorization to merge, tag, or publish.