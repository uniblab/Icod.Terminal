# Icod.Terminal 1.11.0 Persistent Raster Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add bounded, session-owned persistent raster resources and placements above the existing Kitty Graphics backend while preserving the 1.7/1.8 ephemeral raster contract, the 1.10 capability-planning model, the one-reader query architecture, and the stable 1.x compatibility floor.

**Architecture:** Keep public identities opaque. A `TerminalSession` owns a bounded persistent-raster registry. Resource creation uploads raw raster data through direct Kitty transfer, correlates the acknowledgement through the authoritative input/query path, and returns a `TerminalRasterResource` only after terminal-side identity is known. Placements are child handles with private placement IDs; update replaces the same placement at the current cursor position. Lifecycle generation changes make all persistent identities stale rather than replaying hidden source data.

**Tech Stack:** C# 13, .NET `net8.0`/`net9.0`/`net10.0`, xUnit, existing APC/Kitty Graphics encoder/parser, `TerminalInputCoordinator`, `TerminalQueryTransactionManager`, semantic capability/evidence routing, PowerShell packaging verification, GitHub Actions Staging matrix.

**Spec:** `docs/superpowers/specs/2026-09-11-1.11.0-persistent-raster-design.md` and `Icod.Terminal-1.11.0-Development-Roadmap.md`.

## Global Constraints

- Stable compatibility floor remains `1.0.0`.
- Preserve all existing public members and enum numerics; append `TerminalCapability.PersistentRasterGraphics = 9` only.
- `RasterGraphics` remains independently usable through verified Sixel or Kitty Graphics.
- `PersistentRasterGraphics` is satisfied only by the reviewed persistent-capable Kitty path.
- No public Kitty image id, image number, placement id, backend selector, APC writer, or registry enumeration.
- One live `TerminalSession` remains the authoritative input/query reader.
- Persistent acknowledgements reuse existing bounded response routing; no graphics-specific second reader.
- Direct Kitty transfer only; no file, temporary-file, or shared-memory media.
- Caller cancellation is effective before commitment; committed multi-frame transfers are not intentionally truncated.
- No automatic replay, backend switching, or hidden raster retention after ambiguous failure or lifecycle invalidation.
- Maximum live persistent resources per session: `256`.
- Maximum live placements per session: `4096`.
- `TerminalRasterPlacementOptions.Columns` and `.Rows` are nullable and, when present, must be within `1..16384`.
- Public persistent operations use the normal session-output gate. Internal disposal cleanup may use the control-output gate after public output has been closed.
- Current `Icod.DCurses` acceptance and all three package TFMs remain release gates.

---

### Task 1: Activate the 1.11 development line and freeze C110

**Files:**
- Modify: `Directory.Build.props`
- Modify: `Icod.Terminal-1.11.0-Development-Roadmap.md`
- Create: `docs/C110-Persistent-Raster-Architecture-and-API-Regret-Gate.md`

**Interfaces:**
- Consumes: approved 1.11 design
- Produces: explicit `1.11.0-alpha.1` development identity and C110 acceptance record

- [ ] Change `VersionPrefix` to `1.11.0` and `VersionSuffix` to `alpha.1`; do not alter dependency versions.
- [ ] Update roadmap status to C110 complete / C111 active and record the approved public API, registry ceilings, and exclusions.
- [ ] Record C110 as design-only acceptance; no production runtime delta belongs in this commit.
- [ ] Commit: `docs: accept C110 persistent raster contract`.

### Task 2: C111 RED — persistent Kitty protocol vectors

**Files:**
- Create: `tests/Icod.Terminal.Tests/src/Graphics/KittyGraphicsPersistentEncoderTests.cs`
- Create: `tests/Icod.Terminal.Tests/src/Graphics/KittyGraphicsPersistentResponseMatcherTests.cs`
- Modify: `tests/Icod.Terminal.Tests/src/Graphics/KittyGraphicsCodecTests.cs` only for shared response semantics that belong to the codec

**Interfaces:**
- Consumes: existing `KittyRasterAdapter`, `ApcWriter`, `KittyGraphicsResponse`
- Produces: failing byte-exact contract for transmit-only upload, placement, replacement, deletion, and image-number correlation

- [ ] Add RED tests for one-pixel transmit-only upload. Required first payload shape:

```text
Ga=t,f=24,s=1,v=1,t=d,I=31,m=0,q=2;AAAA
```

- [ ] Add RED tests proving multi-frame continuation remains `Gm=1|0,q=2;` and every Base64 data chunk stays within 4096 encoded bytes.
- [ ] Add RED tests for placement payload using terminal image id and private placement id, current-cursor placement, `C=1`, and optional `c`/`r` keys.
- [ ] Add RED tests for replacement: same image id + same placement id, no new public identity.
- [ ] Add RED tests for targeted placement deletion and resource-data deletion; zero image/placement identities must throw before encoding.
- [ ] Add RED matcher tests: an APC response matches only when `I=<expected image number>` is present and the parsed response also contains the terminal-assigned nonzero `i=<image id>`.
- [ ] Add malformed/duplicate/overflow vectors and prove unrelated APC does not match.
- [ ] Run:

```text
dotnet test tests/Icod.Terminal.Tests/Icod.Terminal.Tests.csproj -c Staging -f net10.0 --filter "FullyQualifiedName~KittyGraphicsPersistent"
```

Expected result: compile/test failure because persistent encoder/matcher types do not exist yet.
- [ ] Commit RED tests: `test: define C111 persistent Kitty protocol contract`.

### Task 3: C111 GREEN — typed persistent Kitty protocol foundation

**Files:**
- Create: `src/Graphics/KittyGraphicsPersistentEncoder.cs`
- Create: `src/Graphics/KittyGraphicsPersistentResponseMatcher.cs`
- Modify: `src/Graphics/KittyGraphicsCodec.cs` only where parsing/correlation needs a reusable bounded helper
- Test: files from Task 2 plus existing `KittyGraphicsDirectEncoderTests.cs` and `KittyGraphicsCodecTests.cs`

**Interfaces:**
- Consumes: C111 RED vectors
- Produces: internal typed payload encoders/matcher with no public API

- [ ] Implement `EncodeUploadPayloads(KittyRasterData raster, uint imageNumber)` as a separate path; do not change `EncodeDisplayPayloads(...)` or existing ephemeral bytes.
- [ ] Implement placement/update encoder taking `imageId`, `placementId`, and nullable `columns`/`rows`; emit `a=p`, `C=1`, `i`, `p`, and optional `c`/`r` in one canonical order.
- [ ] Implement typed delete payloads for one placement and terminal resource data.
- [ ] Validate all numeric identities at method entry; zero is invalid.
- [ ] Implement `KittyGraphicsPersistentResponseMatcher : ITerminalResponseMatcher, ICorrelatedTerminalResponseMatcher` keyed by image number. Complete-frame matching must parse with `KittyGraphicsCodec.ParseResponse(...)` and require the expected `ImageNumber` plus nonzero `ImageId`.
- [ ] Preserve existing response ceilings (`MaximumControlDataBytes`, `MaximumResponseMessageBytes`) and correlated malformed-response ownership semantics.
- [ ] Run focused C111 tests plus existing Kitty graphics encoder/codec tests on net8/net9/net10.
- [ ] Commit: `feat: add C111 persistent Kitty protocol foundation`.

### Task 4: C112 RED/GREEN — persistent capability planning

**Files:**
- Modify: `src/Session/TerminalCapabilityStatus.cs`
- Modify: `src/Routing/TerminalControlLanguageVocabulary.cs`
- Modify: `src/Routing/TerminalSemanticBackendRegistry.cs`
- Modify: `src/Session/TerminalSession.CapabilityInspection.cs`
- Modify: `tests/Icod.Terminal.Tests/src/Session/TerminalSessionCapabilityInspectionTests.cs`
- Modify: `tests/Icod.Terminal.Tests/src/Session/TerminalCapabilityVerificationContractTests.cs`
- Modify: `tests/Icod.Terminal.Tests/src/Session/TerminalCapabilityLifecycleAndOwnershipTests.cs`

**Interfaces:**
- Consumes: existing `ApcKittyGraphics` evidence/probe
- Produces: public `PersistentRasterGraphics = 9` and internal semantic routing to Kitty only

- [ ] RED: assert exact public numeric map `ClipboardRead=0 ... RasterGraphics=8, PersistentRasterGraphics=9`.
- [ ] RED: inspect with only verified Sixel: `RasterGraphics=Verified`, `PersistentRasterGraphics` must remain non-verified.
- [ ] RED: verified Kitty evidence must make both raster semantic operations `Verified` without emitting bytes during inspection.
- [ ] RED: redirected output must report persistent endpoint `Unavailable` without probing.
- [ ] RED: verification of persistent raster reuses the bounded Kitty support path and never probes Sixel.
- [ ] GREEN: append `PersistentRasterGraphics` to both public and internal semantic enums; map it only to `ApcKittyGraphics` in the registry.
- [ ] GREEN: add capability mapping and `VerifyCapabilityAsync` case calling `ProbeKittyGraphicsSupportAsync(...)`.
- [ ] Run all capability-planning tests and semantic-routing registry tests.
- [ ] Commit: `feat: add C112 persistent raster capability planning`.

**Public API baseline note:** the new public enum value causes the 1.10 baseline gate to fail intentionally. At this point create `docs/Public-API-Baseline-1.11.md` / `.sha256` from the generated identical multi-TFM snapshot and point `packaging/VerifyPublicApiBaseline.ps1` at 1.11. Treat any additional public API drift after the frozen C110 surface as a review failure.

### Task 5: C113 RED/GREEN — bounded persistent ownership registry

**Files:**
- Create: `src/Graphics/TerminalPersistentRasterRegistry.cs`
- Create: `src/Graphics/TerminalPersistentRasterResourceState.cs`
- Create: `src/Graphics/TerminalPersistentRasterPlacementState.cs`
- Create: `tests/Icod.Terminal.Tests/src/Graphics/TerminalPersistentRasterRegistryTests.cs`
- Modify: `src/Session/TerminalSession.cs` only to initialize/own the registry if constructor ownership is required

**Interfaces:**
- Consumes: session generation concept and private Kitty identities
- Produces: bounded local ownership independent of terminal I/O

- [ ] RED: allocate nonzero image numbers; no collisions among live resources; deterministic wraparound behavior.
- [ ] RED: enforce 256 live resources; allocation #257 returns controlled capacity failure without protocol output.
- [ ] RED: allocate nonzero placement IDs; no collisions; enforce 4096 total live placements.
- [ ] RED: multiple placements may belong to one resource; parent closure blocks child creation.
- [ ] RED: local disposal/unregistration is idempotent and prevents identity reuse while ownership remains live.
- [ ] GREEN: implement registry under one focused synchronization boundary. Do not store `TerminalRasterImage`/pixel data.
- [ ] GREEN: expose only internal state objects/tokens to later session code.
- [ ] Run registry tests across all TFMs; no terminal transport fixtures should be required.
- [ ] Commit: `feat: add C113 persistent raster ownership registry`.

### Task 6: C114 RED — public resource contract and acknowledged upload transaction

**Files:**
- Create: `src/Graphics/TerminalRasterResource.cs`
- Create: `src/Session/TerminalSession.PersistentRasterGraphics.cs`
- Create: `src/Graphics/KittyGraphicsPersistentUploadTransaction.cs`
- Create: `tests/Icod.Terminal.Tests/src/Graphics/TerminalRasterResourceContractTests.cs`
- Create: `tests/Icod.Terminal.Tests/src/Session/TerminalSessionPersistentRasterUploadTests.cs`

**Interfaces:**
- Consumes: C111 encoder/matcher, C112 capability, C113 registry
- Produces: frozen public `CreateRasterResourceAsync(...)` surface and failing end-to-end upload behavior tests

- [ ] RED reflection tests for exactly the C110 public resource/session signatures and no public numeric/backend identities.
- [ ] RED behavior: null image rejected; pre-cancel emits nothing; unavailable endpoint returns controlled `Unavailable`; verified Sixel-only returns `Unsupported`/unavailable persistent result without Sixel output.
- [ ] RED behavior: successful Kitty upload sends transmit-only frames, receives matching `I=<number>,i=<terminal-id>;OK`, then returns `Available(resource)`.
- [ ] RED: wrong image number remains unrelated input/response; malformed correlated ack throws `FormatException`; timeout throws `TimeoutException`; transport failure propagates.
- [ ] RED: caller cancellation after first committed upload frame does not truncate remaining chunks.
- [ ] RED: failed/ambiguous creation never publishes a usable resource handle.
- [ ] Commit RED tests separately before implementation.

### Task 7: C114 GREEN — upload/query coordination and public resource creation

**Files:** same as Task 6, plus minimal focused changes to query/input internals if required.

- [ ] Implement `TerminalRasterResource` as sealed `IAsyncDisposable` with internal session/state constructor only.
- [ ] Implement `TerminalSession.CreateRasterResourceAsync(...)` using `TerminalControlResult<TerminalRasterResource>`.
- [ ] Probe/require `PersistentRasterGraphics` before allocation/output; reserve registry capacity before emission and roll back reservation on failure.
- [ ] Coordinate response ownership through the existing `TerminalInputCoordinator`/query machinery. If multi-frame emission cannot use `ExecuteQueryAsync(...)` directly, add one internal query-manager primitive that registers/arms a normal response expectation, holds the ambiguity gate, executes a supplied committed emitter under the existing control-output gate, and then reuses the existing timeout/late-response ownership path. Do **not** add a second reader or a parallel response router.
- [ ] After first upload frame commits, use `CancellationToken.None` for remaining frames/flush; caller cancellation may still end the caller wait only according to established query late-response ownership semantics.
- [ ] Parse ack, require matching image number and nonzero terminal image id, then bind the reserved registry state and return the public resource.
- [ ] Well-formed Kitty negative response -> controlled `Failed`; correlated `ENOENT` during creation -> controlled `Unavailable` and release reservation.
- [ ] Run focused upload/resource tests plus all existing query and Kitty output tests.
- [ ] Commit: `feat: implement C114 persistent raster resource upload`.

### Task 8: C115/C116 RED/GREEN — placement options, creation, update, and disposal

**Files:**
- Create: `src/Graphics/TerminalRasterPlacementOptions.cs`
- Create: `src/Graphics/TerminalRasterPlacement.cs`
- Extend: `src/Graphics/TerminalRasterResource.cs`
- Extend: `src/Session/TerminalSession.PersistentRasterGraphics.cs`
- Create: `src/Graphics/KittyGraphicsPersistentMutationTransaction.cs`
- Create: `tests/Icod.Terminal.Tests/src/Graphics/TerminalRasterPlacementOptionsTests.cs`
- Create: `tests/Icod.Terminal.Tests/src/Session/TerminalSessionPersistentRasterPlacementTests.cs`

**Interfaces:**
- Consumes: acknowledged resource state
- Produces: frozen C110 placement public API and deterministic child ownership

- [ ] RED: `Columns`/`Rows` null accepted; `0`, negative, and `>16384` rejected before output.
- [ ] RED: `CreatePlacementAsync` emits `a=p,C=1,i=<image>,p=<private>` plus optional `c`/`r`; multiple placements on one resource use distinct private IDs.
- [ ] RED: `UpdateAsync` reuses the same placement ID and current cursor; no public coordinate API appears.
- [ ] RED: disposed handle throws `ObjectDisposedException`; stale current-generation loss returns controlled `Unavailable` before output.
- [ ] RED: one placement dispose emits at most one targeted delete; repeated disposal emits nothing.
- [ ] RED: resource disposal closes children before resource data and releases local ownership even if a delete fails; aggregate multiple cleanup failures.
- [ ] GREEN: implement options validation, public placement object, child creation/update, and typed delete transactions.
- [ ] For normal public mutations use `AcquireSessionOutputAsync`; for internal resource/placement cleanup during session teardown use `AcquireControlOutputAsync(CancellationToken.None)` after local mutation admission has been closed.
- [ ] Map Kitty `ENOENT` to object invalidation + controlled `Unavailable`; other negative responses to `Failed`.
- [ ] Run focused placement tests, existing output-serialization tests, and all graphics tests.
- [ ] Commit C115 and C116 as separate logical commits if the tests naturally split; otherwise one reviewed combined commit is acceptable only if the RED evidence is retained in history.

### Task 9: C117 RED/GREEN — lifecycle invalidation and session teardown

**Files:**
- Modify: `src/Session/TerminalSession.cs`
- Modify: `src/Session/TerminalSession.Lifecycle.cs`
- Extend: persistent registry/state files
- Create: `tests/Icod.Terminal.Tests/src/Session/TerminalPersistentRasterLifecycleTests.cs`

**Interfaces:**
- Consumes: complete persistent resource/placement lifecycle
- Produces: generation-scoped certainty and teardown ordering

- [ ] RED: `InvalidateState()` makes every live resource/placement stale; no delete/replay output is emitted by invalidation itself.
- [ ] RED: managed suspend invalidates persistent identities before host state is yielded; resume does not replay image data.
- [ ] RED: stale resource `CreatePlacementAsync` and stale placement `UpdateAsync` return controlled `Unavailable` without terminal output.
- [ ] RED: stale disposal is local-only and sends no stale Kitty IDs.
- [ ] RED: current-generation `TerminalSession.DisposeAsync()` stops admission of new public mutations, drains accepted committed output, deletes child placements before resource data, then continues existing input/presentation/restoration cleanup.
- [ ] RED: persistent cleanup exceptions are aggregated with existing restoration exceptions.
- [ ] GREEN: add `InvalidatePersistentRasterState()` to explicit invalidation and suspend boundary. Avoid double-advancing semantic evidence generation: persistent object generation is a registry generation separate from the existing semantic evidence generation advanced by input-protocol invalidation.
- [ ] GREEN: call persistent close from session disposal after `StopAcceptingSessionOutput()` but before final host restoration; cleanup must use internal control-output acquisition so teardown remains possible while public writes are rejected.
- [ ] Run lifecycle tests, full session disposal tests, and existing Kitty committed-output disposal test.
- [ ] Commit: `feat: integrate C117 persistent raster lifecycle`.

### Task 10: C118 adversarial, sample, package, and downstream acceptance

**Files:**
- Create: `samples/Icod.Terminal.PersistentRasterGraphics.Sample/Icod.Terminal.PersistentRasterGraphics.Sample.csproj`
- Create: `samples/Icod.Terminal.PersistentRasterGraphics.Sample/Program.cs`
- Modify: `samples/README.md`
- Modify: `Icod.Terminal.sln`
- Create: `packaging/VerifyPersistentRasterGraphicsSample.ps1`
- Create: `tools/package-persistent-raster-graphics-smoke/Icod.Terminal.PackagePersistentRasterGraphicsSmoke.csproj`
- Create: `tools/package-persistent-raster-graphics-smoke/Program.cs`
- Create: `packaging/VerifyPersistentRasterGraphicsPackage.ps1`
- Modify: `packaging/VerifyRuntime.ps1`
- Modify: `packaging/VerifyPackageContractShard.ps1`
- Add/extend hardening tests under `tests/Icod.Terminal.Tests/src/Graphics/` and `src/Session/`

**Interfaces:**
- Consumes: complete 1.11 runtime/public surface
- Produces: consumer examples and multi-TFM package/downstream evidence

- [ ] Sample must inspect/verify `PersistentRasterGraphics`, create one small generated raw raster resource, create a placement, update it after moving cursor through normal terminal APIs, and dispose via `await using`; it must not branch on Kitty/Sixel/brand/TERM or display numeric IDs.
- [ ] Add sample verifier for net8/net9/net10 and wire it into `VerifyRuntime.ps1`.
- [ ] Add fresh NuGet-only smoke with exactly one direct package reference: `Icod.Terminal`; let NuGet resolve transitive dependencies.
- [ ] Verify generated XML docs include `TerminalRasterResource`, `TerminalRasterPlacement`, `TerminalRasterPlacementOptions`, `CreateRasterResourceAsync`, `CreatePlacementAsync`, and `UpdateAsync`.
- [ ] Add package verifier to the semantic package-contract shard.
- [ ] Add adversarial tests for response oversize/malformed fields/wrong correlation, 256/4096 exhaustion, wraparound, repeated invalidation/disposal, race boundaries, and transport failure at meaningful commitment points.
- [ ] Keep current DCurses acceptance and hardening soak unchanged unless a real incompatibility is exposed.
- [ ] Run `./packaging/VerifyRuntime.ps1 -Configuration Staging` and the package semantic shard on the exact branch head.
- [ ] Commit: `test: qualify C118 persistent raster consumers` plus any narrowly required runtime fixes in separate commits.

### Task 11: C119 public API, permanent docs, and stable release closure

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`
- Modify: `Icod.Terminal.csproj`
- Modify: `Directory.Build.props`
- Modify: `Icod.Terminal-Development-Roadmap.md`
- Modify: `Icod.Terminal-1.11.0-Development-Roadmap.md`
- Modify: `docs/Architecture.md`
- Modify: `docs/Security-and-Privacy.md`
- Modify: `docs/Compatibility-and-Versioning.md`
- Create: `docs/Persistent-Raster-Resources-and-Placements.md`
- Create: `docs/Public-API-Baseline-1.11.md`
- Create/update: `docs/Public-API-Baseline-1.11.sha256`
- Create: `docs/releases/1.11.0.md`
- Create: `docs/C119-1.11.0-Release-Closure.md`
- Modify: `packaging/VerifyPublicApiBaseline.ps1`

**Interfaces:**
- Consumes: accepted C111-C118 implementation
- Produces: frozen stable `1.11.0` release candidate

- [ ] Freeze one identical public API snapshot for net8/net9/net10. Confirm the delta is exactly the C110-approved capability value/types/methods and no public Kitty identifiers leaked.
- [ ] Remove prerelease suffix and set stable package identity `1.11.0`.
- [ ] Update package release notes and package tags for persistent raster ownership without branch-specific alpha links.
- [ ] Document permanent ownership/lifecycle/security semantics and explicit exclusions.
- [ ] Close the 1.11 roadmap and hand the long-range roadmap to the conditional 1.12 advanced-placement track.
- [ ] Run the full unchanged-head PR Staging matrix: Windows, Linux, macOS, package candidate, all package shards, validated package artifact.
- [ ] Record the exact final head SHA/workflow only after every job is green.
- [ ] Leave merge, mainline Release validation, `v1.11.0` tag, GitHub Release, and NuGet publication as explicit maintainer actions.
- [ ] Commit: `docs: close Icod.Terminal 1.11.0`.

## Execution Order and Review Gates

```text
C110 accepted contract
  -> C111 protocol substrate (no public API)
  -> C112 capability addition + provisional 1.11 API baseline
  -> C113 local bounded ownership
  -> C114 acknowledged resource upload
  -> C115/C116 placement ownership and cleanup
  -> C117 lifecycle/teardown
  -> C118 adversarial/package/downstream qualification
  -> C119 final API/docs/release closure
```

At every tranche boundary, preserve RED-before-GREEN evidence in commit history where practical and do not broaden scope merely because the Kitty protocol offers additional features. Source rectangles, z-order, Unicode placeholders, relative placement, pixel offsets, animation, file/temp/shared-memory transport, automatic replay, and scene-graph policy remain outside 1.11.