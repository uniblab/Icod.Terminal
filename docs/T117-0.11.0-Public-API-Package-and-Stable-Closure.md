# T117 — 0.11.0 Public API, Package, and Stable Closure

**Release:** `0.11.0`  
**Tranche:** `T117`  
**Current development version:** `0.11.0`  
**Status:** Stable metadata closure complete; exact stable-head validation required

---

## 1. Purpose

T117 closes the OSC 22 terminal mouse-pointer release without expanding its reviewed public surface.

No new protocol family or public API is introduced after T116.

---

## 2. Frozen public delta

The frozen stable 0.11 delta is recorded in `docs/Public-API-Baseline-0.11.md` and is limited to:

- `TerminalPointerShape` with exactly 30 semantic values;
- `TerminalPointerShapeLease` and `Shape`;
- `TerminalPointerShapeObservation` with `HasShape` and `Shape`;
- `TerminalSession.SetPointerShapeAsync(...)`;
- `TerminalSession.ResetPointerShapeAsync(...)`;
- `TerminalSession.AcquirePointerShapeAsync(...)`;
- `TerminalSession.QueryCurrentPointerShapeAsync(...)`;
- `TerminalSession.QueryDefaultPointerShapeAsync(...)`;
- `TerminalSession.QueryGrabbedPointerShapeAsync(...)`;
- `TerminalSession.QueryPointerShapeSupportAsync(...)`.

No raw OSC 22 API, arbitrary pointer-name string, Kitty push/pop API, automatic support flag, or terminal-emulator inference is added.

---

## 3. Source sample closure

T117 adds `samples/Icod.Terminal.PointerShape.Sample` as a normal `Icod.Terminal.sln` project.

The sample demonstrates:

- explicit semantic pointer set and terminal-policy reset;
- nested `Pointer` / `Wait` scoped ownership;
- restoration of the outer Icod-owned shape;
- final reset to terminal policy;
- explicit bounded pointer-support query;
- explicit current-pointer observation;
- timeout reported as unknown support rather than unsupported.

The sample intentionally does not perform emulator detection or claim observed support from successful output alone.

---

## 4. Package-only consumer closure

T117 adds:

- `tools/package-pointer-shape-smoke/Icod.Terminal.PackagePointerShapeSmoke.csproj`;
- `tools/package-pointer-shape-smoke/Program.cs`;
- `packaging/VerifyPointerShapePackage.ps1`.

The fresh package consumer is copied outside the repository project-reference graph and restored only against the newly produced `Icod.Terminal` package plus normal NuGet dependencies.

It validates on net8.0, net9.0, and net10.0:

- CSS `default` semantic set versus empty OSC 22 terminal-policy reset;
- nested `Pointer`, `Text`, and `Wait` owners;
- out-of-order non-controlling disposal is silent;
- controlling disposal restores the newest remaining owner;
- final release emits terminal-policy reset;
- repeated successful disposal is idempotent;
- pointer-shape operations do not implicitly flush;
- all four public pointer query method signatures are available from the packaged assembly.

---

## 5. XML documentation gate

The fresh package must contain generated XML documentation for every supported TFM and include:

- the `TerminalPointerShape` type;
- all 30 public enum members;
- `TerminalPointerShapeLease`, `Shape`, and `DisposeAsync()`;
- `TerminalPointerShapeObservation`, `HasShape`, and `Shape`;
- every new 0.11 `TerminalSession` pointer method.

Missing documentation is a release-blocking package failure.

T117 also completes public query documentation for timeout, cancellation, malformed response, endpoint requirements, and no-shape semantics.

---

## 6. Retained historical gates

0.11 retains rather than replaces:

- general package structure verification;
- 0.8 cursor-style package/XML contract verification;
- 0.9 synchronized-output package/XML contract verification;
- 0.10 terminal-progress package/XML contract verification;
- real downstream DCurses synchronized-output acceptance;
- real downstream DCurses terminal-progress acceptance.

T116 additionally supplies the real DCurses terminal pointer-shape acceptance gate.

---

## 7. Documentation and metadata

Stable metadata closure is complete:

- `VersionPrefix` is `0.11.0`;
- `VersionSuffix` is empty;
- effective package version is `0.11.0`;
- assembly version remains `0.11.0.0`;
- root README identifies 0.11.0 as the stable release and uses the stable installation command;
- `samples/README.md` documents the pointer-shape sample;
- package release notes and OSC 22/pointer-shape tags are final;
- `docs/Public-API-Baseline-0.11.md` is frozen as the stable public surface;
- the 0.11 development roadmap records T110–T117 closure.

The stable bump was justified by cumulative alpha.7 workflow #487, which passed Windows/Linux/macOS source validation, all three DCurses acceptance gates, exact package verification, retained 0.8/0.9/0.10 package contracts, and the new 0.11 package/XML contract.

---

## 8. Alpha.7 stable-version gate — satisfied

Cumulative exact alpha.7 head `188369aa13fc010c0e0af97a28241b3bf74e5113` passed workflow #487.

That evidence includes:

- Windows solution restore/build/test;
- Linux solution restore/build/test;
- macOS solution restore/build/test;
- DCurses synchronized-output acceptance;
- DCurses terminal-progress acceptance;
- DCurses pointer-shape acceptance;
- exact package construction/verification;
- 0.8 cursor-style package gate;
- 0.9 synchronized-output package gate;
- 0.10 terminal-progress package gate;
- 0.11 pointer-shape package/XML gate.

This satisfied the requirement for emptying `VersionSuffix`.

---

## 9. Stable candidate gate — current

The exact stable PR head produced by the metadata/status closure must pass the same complete matrix again.

No feature, public API, protocol behavior, or package-contract expansion belongs between that stable head and merge.

---

## 10. Post-merge release gate

After PR merge:

1. validate the resulting exact `main` commit under Release/distribution configuration;
2. require the complete solution, all three downstream DCurses gates, and all package-contract gates to pass;
3. only then create `v0.11.0`;
4. tagged release workflow rebuilds, retests, repacks, selects the exact tagged package, reruns package/downstream acceptance, and only then publishes.

---

## 11. T117 decision

T117 implementation and stable metadata closure are complete.

The remaining work is validation/release only:

```text
alpha.7 workflow #487 green
    -> stable metadata/status wording complete
    -> exact stable PR head green
    -> merge to main
    -> exact main Release validation green
    -> v0.11.0
```
