# T87 — Icod.Terminal 0.8.0 Public API, Package, and Stable Closure

**Release:** `0.8.0`  
**Tranche:** `T87`  
**Status:** Implemented; exact stable-head validation required before merge  
**Theme:** public API regret audit, documentation, sample, package-only consumer validation, and stable release gates

## 1. Public API closure

The 0.8 public delta is frozen in [`Public-API-Baseline-0.8.md`](Public-API-Baseline-0.8.md).

Stable additions are:

```text
TerminalCursorStyle
TerminalCursorStyleObservation
TerminalCursorStyleLease
TerminalSession.SetCursorStyleAsync(...)
TerminalSession.QueryCursorStyleAsync(...)
TerminalSession.AcquireCursorStyleAsync(...)
```

No additional public generic CSI or cursor-style protocol surface is accepted for 0.8.

## 2. Stable semantic contract

The six semantic styles remain:

```text
BlinkingBlock     -> DECSCUSR 1
SteadyBlock       -> DECSCUSR 2
BlinkingUnderline -> DECSCUSR 3
SteadyUnderline   -> DECSCUSR 4
BlinkingBar       -> DECSCUSR 5
SteadyBar         -> DECSCUSR 6
```

Inbound omitted/0/1 normalize to blinking block. Positive state outside the frozen set, including xterm parameter 7, is not coerced to a style.

Cursor style remains independent of cursor visibility.

## 3. Restoration closure

Scoped restoration is accepted only because the outermost lease observes an actual prior semantic style through DECRQSS before mutation.

The stable lease contract is:

- no mutation when prior state cannot be observed successfully;
- strict-LIFO nested ownership;
- nested acquisition reuses known library-owned state;
- out-of-order release fails without physical-state mutation;
- release failure retains ownership for retry/cleanup;
- suspend restores the observed baseline;
- resume reapplies the active innermost logical style;
- release while suspended emits no style bytes;
- disposal performs final best-effort baseline restoration;
- no guessed reset, hard-coded style, or xterm parameter 7 is used for restoration.

## 4. Documentation and sample closure

The repository README now documents 0.8 as the current stable release and describes:

- setter semantics;
- explicit observation;
- scoped restoration;
- bar-style compatibility posture;
- cursor-style/visibility independence;
- stable release validation flow.

A focused multi-target sample was added under:

```text
samples/Icod.Terminal.CursorStyle.Sample/
```

It demonstrates explicit observation, unsupported/timeout handling, and a scoped cursor-style lease.

## 5. Package XML documentation gate

`packaging/VerifyCursorStylePackage.ps1` opens the freshly produced `Icod.Terminal.<version>.nupkg` and requires the 0.8 XML documentation members in each package TFM:

```text
net8.0
net9.0
net10.0
```

The required package documentation includes the enum, observation type/properties, lease type/property/disposal method, setter, query, and scoped-acquisition method.

## 6. Fresh package-only consumer gate

`tools/package-cursor-style-smoke` references `Icod.Terminal` exclusively as a NuGet package.

The verification script copies the consumer to a temporary clean directory, configures a package source pointing at the newly produced artifact directory, uses a fresh `NUGET_PACKAGES` directory, restores without cache, and runs under:

```text
net8.0
net9.0
net10.0
```

The consumer exercises the public setter, typed query, observation object, scoped lease, and restoration behavior against scripted terminal I/O.

This proves that the packaged public surface—not merely the project-reference build—is consumable.

## 7. CI and release integration

The 0.8 package gate is integrated into:

- pull-request Linux package validation;
- `VerifyDistribution.ps1`, therefore the `main` Release architecture matrix;
- tagged release package validation before NuGet.org or GitHub Packages publication.

The focused cursor-style sample is also built explicitly by PR, distribution, and tagged-release validation.

## 8. Stable metadata

Repository version authority is now:

```text
VersionPrefix:   0.8.0
VersionSuffix:   <empty>
Version:         0.8.0
PackageVersion:  0.8.0
AssemblyVersion: 0.8.0.0
```

Package release notes describe typed DECSCUSR cursor-style control, DECRQSS observation, bar-style compatibility, and truthful scoped restoration.

## 9. Release gate

T87 implementation does not itself authorize tagging.

The stable sequence is:

1. exact PR head is green on Windows, Linux, and macOS;
2. Linux Staging package verification, including 0.8 package XML and package-only smoke, is green;
3. merge PR to `main`;
4. exact `main` Release distribution validation is green across the configured architecture matrix;
5. only then create tag `v0.8.0`;
6. the tag workflow rebuilds, retests, repacks, selects the exact 0.8.0 artifact, reruns package verification, and only then publishes.

Until steps 1 through 4 are satisfied, the branch is stable-versioned but not release-authorized.

## 10. T87 decision

The 0.8 feature/API design is closed. Any new cursor protocol family, synchronized-output policy, generic CSI construction, or broader state-transaction abstraction belongs to a later release.

`0.9.0` remains reserved for synchronized output and nested transactional output state.
