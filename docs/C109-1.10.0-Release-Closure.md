# C109 — Icod.Terminal 1.10.0 Release Closure

- **Release:** `Icod.Terminal 1.10.0`
- **Tranche:** C109
- **Status:** release-closure implementation complete; exact-head Staging qualification pending
- **Stable compatibility floor:** `1.0.0`
- **Final public API fingerprint:** `ee705250d19d51df92645e5020f188646dd2dbf38483278e6e57ce6fbbc1e9fb`

## Purpose

C109 closes the 1.10 development line without adding further feature work. Its job is to make the repository, package metadata, permanent authorities, and roadmaps describe the same stable 1.10 contract, then qualify one unchanged final pull-request head before maintainer merge/tag/publication.

## Release-facing state

The release candidate now records:

- repository/package version `1.10.0` with no prerelease suffix;
- stable package release notes in `Icod.Terminal.csproj`;
- stable README installation and capability-planning guidance;
- final `CHANGELOG.md` 1.10 entry;
- curated `docs/releases/1.10.0.md` release notes;
- final public API baseline in `docs/Public-API-Baseline-1.10.md` and `.sha256`;
- permanent capability-planning contract in `docs/Capability-Inspection-and-Planning.md`;
- Architecture authority promoted to first-class 1.10 capability inspection/verification semantics;
- Security/Privacy authority promoted to explicit side-effect-free inspection, bounded explicit verification, fingerprinting, endpoint, lifecycle, and trust rules;
- Compatibility/Versioning authority promoted from the 1.9 baseline to the final 1.10 baseline and frozen 1.10 enum numerics;
- the versioned 1.10 roadmap closed at C109;
- the long-range roadmap handed to the 1.11 persistent raster resource/placement track.

## Feature acceptance carried into C109

C105–C108 were accepted on:

```text
f5272594f31188e4c3f096e61e226c7d550319b6
```

by workflow:

```text
#1518 / 34594556128
```

That acceptance covered:

- lifecycle generation invalidation;
- static-vs-live evidence behavior;
- concurrent side-effect-free inspection;
- pre-cancelled verification with no terminal traffic;
- suspended/closed query ownership;
- unavailable endpoints;
- repeated verification of decisive evidence;
- multi-backend semantic evidence;
- the capability-planning sample;
- fresh NuGet-only package consumption on `net8.0`, `net9.0`, and `net10.0`;
- downstream `Icod.DCurses` acceptance;
- final API fingerprint freeze.

## C109 qualification rule

C109 is accepted only when one unchanged final pull-request head passes the complete Staging matrix, including runtime validation on Windows/Linux/macOS, package candidate/public-API validation, all package-contract shards, and the validated package artifact.

No further feature/API work belongs in this tranche. If qualification exposes a release-closure defect, fix only that defect, update this record as necessary, and rerun qualification on the new exact head.

## Maintainer handoff

After exact-head Staging qualification is green:

1. mark PR #51 ready for maintainer review;
2. merge remains an explicit maintainer action;
3. validate the resulting `main` head through the Release workflow;
4. tag `v1.10.0` only after successful mainline validation;
5. use `docs/releases/1.10.0.md` as the curated GitHub Release/publication notes;
6. NuGet publication remains a maintainer/release-workflow action.

The next feature-development line is `1.11.0`, focused on persistent raster resources and placements above backend-specific terminal identifiers.
